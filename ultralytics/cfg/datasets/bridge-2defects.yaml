

# Train/val/test sets as 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]
path: dataset/yolo_剥落掉角 # dataset root dir
train: images/train # train images (relative to 'path') 6935 images
val: images/val # val images (relative to 'path') 975 images
test:

# Classes
names:
  0: 剥落
  1: 掉角